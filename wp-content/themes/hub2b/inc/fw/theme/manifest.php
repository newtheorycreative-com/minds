<?php if ( ! defined( 'FW' ) ) {
	die( 'Forbidden' );
}

$manifest = array();

$manifest['id'] = 'hub2b';

$manifest['supported_extensions'] = array(
	'backups' => array(),
	'megamenu' => array(),
);
